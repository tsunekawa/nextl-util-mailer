from <%= @comment["user"]["login"] %>(<%= url_for(:user, :login=>@comment["user"]["login"]) %>)
======================================

<%= @comment["body"] %>

======================================
(forwarded from <%= url_for(:issues_comment, :issue_number=> @issue["number"], :comment_id=> @comment["id"]) %>)
